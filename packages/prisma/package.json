{
  "name": "@app/prisma",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist/* && tsc -p tsconfig.json",
    "dev": "npx prisma generate && npx prisma db push",
    "migrations:new": "npx prisma migrate dev --name ",
    "start": "npx prisma migrate deploy",
    "seed": "node ./dist/seed.js"
  },
  "dependencies": {
    "prisma": "3.11.1",
    "@prisma/client": "3.11.1",
    "@faker-js/faker": "^6.1.2"
  },
  "devDependencies": {   
  }
}
